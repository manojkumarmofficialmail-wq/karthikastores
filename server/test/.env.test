# Fixed environment for the unit tests. Loaded after server/.env so the suite
# is deterministic on any machine and never touches a real database — these
# tests exercise pure logic (distance, status flow, signatures) only.
NODE_ENV=test
DATABASE_URL=postgresql://unused:unused@localhost:5432/unused
DATABASE_SSL=false
JWT_SECRET=karthika-test-secret-0000000000000000000000000000
PAYMENT_PROVIDER=simulated
