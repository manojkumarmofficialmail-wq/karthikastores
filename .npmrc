# Vite builds the storefront and lives in devDependencies. Deploy pipelines
# commonly install with NODE_ENV=production, which makes npm skip devDependencies
# entirely — `npm run build` then dies with "vite: not found" (exit 127).
# This forces them in for every install, including `npm ci`.
include=dev
